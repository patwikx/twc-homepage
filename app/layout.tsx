import type React from "react"
import type { Metadata } from "next"
import { DM_Sans } from "next/font/google"
import "./globals.css"

const dmSans = DM_Sans({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-dm-sans",
})

export const metadata: Metadata = {
  title: "Tropicana Worldwide Corporation - Luxury Resort Hotels",
  description:
    "Experience luxury at our four tropical resort destinations: Anchor Hotel, Dolores Farm Resort, Dolores Tropicana Resort, and Dolores Lake Resort.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${dmSans.variable} antialiased`}>
      <body>{children}</body>
    </html>
  )
}
